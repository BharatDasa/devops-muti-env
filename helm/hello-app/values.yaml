image:
  repository: bharatdasa/hello-world
  tag: latest

replicaCount: 2

service:
  port: 80

ingress:
  enabled: true
  host: ""   # overridden per env
  tls:
    enabled: true
    secretName: wildcard-tls

resources: {}

# 🔥 ADD THIS (VERY IMPORT
serviceAccount:
  create: false
  name: hello-app-sa
